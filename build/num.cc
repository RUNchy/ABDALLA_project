#include <iostream>
#include<stdio.h>
#include "bits/stdc++.h"
#include"math.h"
#include<iomanip>
using namespace std;
int main(){
    // std::cout << std::fixed;
    // std::cout << std::setprecision(2);
    // double chan=0.23;
    // int copy_num=10;
    // double pi = 2*acos(0.0);
    // cout<<abs(asin(sqrt(3)/2))*180/pi;
    // // cout<<sin(pi)<<endl;
    double x =0 , y , z=0;
    cout<<x ;
    return 0;
}